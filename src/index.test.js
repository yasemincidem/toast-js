describe("toast", () => {
  it("warning - pass message", () => {
    expect(2==2).toBeTruthy();
  })
  it("warning - no message", () => {
    expect(2==2).toBeTruthy();
  })
  it("warning - no message", () => {
    expect(2==2).toBeTruthy();
  })
});
